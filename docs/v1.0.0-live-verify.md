# Conjure3D v1.0.0 Live Verification Log

Date: 2026-06-30  
Verified by: Claude (automated via sidecar venv Python + live Blender 4.2.21)

---

## Task #11 — Vase chain (`open_top` + `bridge_top_loops`)

**Method:** Direct socket calls to Blender 4.2.21 (Ahuja MCP v1.2 addon, port 9876)  
via `blender_client.py` protocol (`{"type":"execute_code","params":{"code":"..."}}`)

**Test mesh:** Two-loop hollow vase — outer cylinder 100mm (open top), inner cylinder 96mm  
(open top), bottom connecting ring, no top caps — 64 boundary edges, two separate open loops.

| Operation | Result |
|---|---|
| `open_top("vase", top_margin_mm=2.0)` | `boundary_edges: 64, max_z_mm: 98.0` |
| `bridge_top_loops("vase")` | `boundary_edges: 0, watertight: True` |

**Result: PASS**

---

## Task #12 — `color_split.run("quarter")` — 4-wedge partitioning

**Method:** Same Blender connection; UV sphere (R=50mm) as test mesh.

| Check | Result |
|---|---|
| Objects created | 4 (Conjure_Q0–Q3) |
| Volume coverage | **0.00% error** (Σwedge = sphere exactly) |
| Quadrant Q0 | bbox centroid (x+, y+) |
| Quadrant Q1 | bbox centroid (x-, y+) |
| Quadrant Q2 | bbox centroid (x-, y-) |
| Quadrant Q3 | bbox centroid (x+, y-) |
| Distinct quadrants | 4 of 4 |
| Source object removed | Yes |
| Cutter objects removed | Yes |

**Result: PASS** — geometric partition confirmed (volume + distinct-quadrant check), not just object count.

---

## Task #13 — GGUF download vs HF URL

**Method:** `requests.head()` to HF URL; sidecar JSON-RPC via `printf | timeout sidecar.exe` (Bash).

| Check | Result |
|---|---|
| HF URL status | HTTP 200 |
| HF Content-Length | 4,683,073,536 bytes |
| On-disk file size | 4,683,073,536 bytes |
| `llm.model_status` | `{phase: "idle", model_present: true}` |
| `llm.download_start` | `{phase: "checking"}` (thread spawned) |
| `llm.model_status` (poll) | `{phase: "done", model_present: true}` |

**Note:** File already present — thread correctly detected and skipped re-download (no network call).  
URL liveness + byte-count verified live. Streaming/resume/SHA path covered by unit tests.

**Result: PASS**

---

## Summary

| Task | Description | Result |
|---|---|---|
| #11 | Vase chain open_top + bridge_top_loops | **PASS** |
| #12 | color_split quarter — 4-wedge partition | **PASS** |
| #13 | GGUF URL live + file byte-match | **PASS** |

All prerequisites for **Task #14 (Tag v1.0.0)** are green.
