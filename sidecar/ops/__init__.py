# Geometry op modules. Each op builds a Python source string, sends it via
# blender_client.execute_blender_code, and parses a single JSON line of stats
# from the captured stdout. Pipeline.md is the spec.
