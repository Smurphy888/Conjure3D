[project]
name = "conjure3d-sidecar"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "keyring",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "responses",
]
